# 트랜잭션 (Transaction)

---

## 트랜잭션이란?

**함수 종속성**을 이용해 릴레이션을 연관성이 있는 속성들로만 구성되도록 분해하는 것과 달리,  
트랜잭션은 **데이터베이스의 상태를 변환시키는 하나의 논리적 작업 단위**이다.

- 여러 SQL 문을 묶어 하나의 작업으로 처리한다.
- DBMS는 트랜잭션 단위로 Atomic하게 처리되도록 보장한다.

### 트랜잭션이 필요한 이유

수강신청 예시:
- 수강 인원 확인 → 정원 미만이면 enrollments에 삽입
- 이 두 작업 사이에 다른 트랜잭션이 끼어들면 **동시성 문제** 발생
- 트랜잭션으로 묶으면 `FOR UPDATE`로 락을 걸어 순차 처리 보장

---

## 트랜잭션의 특성 — ACID

### Atomicity (원자성)

- 트랜잭션의 모든 연산이 완전히 실행되거나, 하나도 실행되지 않아야 한다 (**all-or-nothing**)
- 트랜잭션 실행 중 장애 발생 시 데이터베이스는 트랜잭션 시작 이전 상태로 복원되어야 한다
- 원자성을 지키기 위해 장애 발생 시 **Rollback**을 수행한다

### Consistency (일관성)

- 트랜잭션이 완료된 후에도 데이터베이스는 항상 일관된 상태를 유지해야 한다
- 트랜잭션 실행 전후로 무결성 제약조건을 만족해야 한다
- 예: 계좌 이체 후 전체 잔액의 합은 이체 전과 동일해야 한다

### Isolation (고립성)

- 동시에 실행 중인 트랜잭션들은 서로의 중간 연산 결과를 볼 수 없어야 한다
- 고립성을 완전히 보장하면 동시 실행 트랜잭션이 순차적으로 실행된 것과 동일한 결과를 보장하지만, 성능 저하가 발생할 수 있다
- 고립성 수준(Isolation Level)을 조절하여 성능과 일관성 사이의 트레이드오프를 관리한다

### Durability (지속성)

- 트랜잭션이 완료되면 그 결과는 시스템 장애가 발생하더라도 데이터베이스에 영구적으로 반영되어야 한다
- 지속성을 보장하기 위해 장애 발생 시 **Recovery** 기법을 사용한다

---

## 트랜잭션 연산

### Commit

- 트랜잭션의 모든 연산이 정상적으로 완료되었음을 선언하는 연산
- Commit 연산이 실행되면 트랜잭션의 모든 변경 내용이 데이터베이스에 영구적으로 반영된다

### Rollback

- 트랜잭션 실행을 취소하는 연산
- Rollback 연산이 실행되면 이전에 수행한 모든 연산이 취소되고, 데이터베이스는 트랜잭션 시작 이전 상태로 복원된다

```sql
BEGIN;
  SELECT ...;
  INSERT ...;
  UPDATE ...;
COMMIT;   -- 또는 ROLLBACK;
```

---

## 트랜잭션의 상태

| 상태 | 설명 |
|------|------|
| **Active (활동)** | 트랜잭션이 실행 중인 초기 상태 |
| **Partially Committed (부분 완료)** | 트랜잭션의 마지막 연산이 실행된 직후의 상태 |
| **Committed (완료)** | 트랜잭션이 성공적으로 완료되어 commit 연산을 실행한 상태. 트랜잭션의 모든 변경 내용이 데이터베이스에 반영되고, 트랜잭션이 종료된다 |
| **Failed (실패)** | 장애로 인해 트랜잭션의 정상적인 실행이 불가능한 상태 |
| **Aborted (철회)** | 트랜잭션이 취소되어 rollback 연산을 실행한 상태. 이전에 수행한 모든 연산이 취소되고 트랜잭션 시작 이전 상태로 복원된다. 이 상태가 된 트랜잭션은 두 가지 선택지가 있다: 재시작 또는 폐기 |

```
Active
  ↓ 마지막 연산 실행
Partially Committed
  ↓ 정상 완료        ↓ 장애 발생
Committed          Failed
                     ↓ rollback
                   Aborted
```

---

## 단순 트랜잭션 모델 (Simple Transaction Model)

트랜잭션은 두 가지 수준에서 데이터를 다룬다.

### 디스크(보조기억장치) ↔ 버퍼(주기억장치) 연산

| 연산 | 설명 |
|------|------|
| **input(X)** | 디스크의 X가 포함된 블록을 주기억장치 버퍼로 읽어옴 |
| **output(X)** | 주기억장치 버퍼의 X가 포함된 블록을 디스크에 씀 |

- 데이터 단위: **블록(block)**
  - 디스크 블록: 디스크에 있는 블록
  - 버퍼 블록: 주기억장치에 있는 블록

### 버퍼(주기억장치) ↔ 트랜잭션(프로그램 변수) 연산

| 연산 | 설명 |
|------|------|
| **read(X)** | 버퍼 블록에서 데이터 항목 X를 트랜잭션의 로컬 변수로 복사 |
| **write(X)** | 트랜잭션의 로컬 변수 값을 버퍼 블록의 X에 씀 |

> 각 트랜잭션은 데이터베이스의 데이터를 접근하기 전에, 주기억장치 버퍼 블록에 있는 데이터를 자신의 로컬 변수로 복사하고, 수정 후에 결과를 다시 버퍼 블록에 반영한다.

---

## Transaction Isolation (트랜잭션 고립성)

### 동시성 제어 (Concurrency Control)

트랜잭션 처리 시스템은 여러 트랜잭션을 동시에 실행할 수 있다.

**동시 실행을 허용하는 이유:**
- **처리량(throughput) 및 자원 이용률 향상**: 여러 트랜잭션이 CPU와 디스크 접근을 공유
- **대기 시간 감소**: 직렬 실행 시 짧은 트랜잭션이 긴 트랜잭션을 기다려야 하지만, 동시 실행 시 서로 다른 데이터를 처리하는 트랜잭션들은 병렬로 실행 가능. 평균 응답 시간(트랜잭션 제출 후 완료까지의 평균 시간)을 단축

---

## 스케줄 (Schedule)

**스케줄**: 여러 트랜잭션에 속한 연산들의 실행 순서

### 직렬 스케줄 (Serial Schedule)

- 트랜잭션들이 **겹치지 않고 순차적으로** 실행되는 스케줄
- 한 트랜잭션이 완전히 끝난 후 다음 트랜잭션이 시작된다
- 항상 일관된 상태를 보장하지만, 동시 실행이 없어 성능이 낮다
- n개의 트랜잭션에 대해 **n! 개**의 유효한 직렬 스케줄 존재

> **단점**: 직렬 스케줄만 허용하면 데이터베이스의 CPU가 한 번에 하나의 트랜잭션만 처리하므로, 전체 처리량이 낮고 대기 시간이 길어진다

### 동시 스케줄 (Concurrent Schedule)

- 트랜잭션들이 **겹쳐서** 실행되는 스케줄
- 한 트랜잭션이 완료되기 전에 다른 트랜잭션의 연산이 실행될 수 있다
- 성능은 높지만, 잘못된 동시 실행 시 데이터 불일치 발생 가능

---

## Serializability (직렬 가능성)

- 직렬 스케줄은 항상 직렬 가능하다
- 여러 트랜잭션의 연산이 인터리브될 때, 해당 스케줄이 직렬 가능한지 판단해야 한다
- **직렬 가능한 스케줄 = 어떤 직렬 스케줄과 동등한 동시 스케줄**

---

## Conflict (충돌)

동일한 데이터 항목 Q를 참조하는 두 연산 I, J가 있을 때:

| 경우 | 충돌 여부 | 이유 |
|------|-----------|------|
| I = read(Q), J = read(Q) | ❌ 충돌 없음 | 순서에 무관하게 같은 값을 읽음 |
| I = read(Q), J = write(Q) | ✅ 충돌 | 순서에 따라 Ti가 읽는 값이 달라짐 |
| I = write(Q), J = read(Q) | ✅ 충돌 | 순서에 따라 Tj가 읽는 값이 달라짐 |
| I = write(Q), J = write(Q) | ✅ 충돌 | 마지막으로 쓴 값만 남으므로 최종 결과가 달라짐 |

> **핵심**: 두 연산 중 하나라도 **write**이면 충돌

---

## Conflict Serializability (충돌 직렬 가능성)

- 두 스케줄이 **충돌 동등(Conflict Equivalent)** 하다: 비충돌 연산들의 순서를 교환하여 한 스케줄을 다른 스케줄로 변환 가능할 때
- 스케줄 S가 **충돌 직렬 가능(Conflict Serializable)**: S가 어떤 직렬 스케줄과 충돌 동등할 때

---

## 선행 그래프 (Precedence Graph)

충돌 직렬 가능성을 판단하는 방법.

**구성:**
- **정점(V)**: 스케줄에 참여하는 모든 트랜잭션
- **간선(E)**: 다음 세 조건 중 하나를 만족하는 Ti → Tj 방향 간선

| 조건 | 설명 |
|------|------|
| Ti가 write(Q) 후 Tj가 read(Q) | Ti의 쓰기 결과를 Tj가 읽음 |
| Ti가 read(Q) 후 Tj가 write(Q) | Ti가 읽은 값을 Tj가 덮어씀 |
| Ti가 write(Q) 후 Tj가 write(Q) | Ti의 쓰기 결과를 Tj가 덮어씀 |

**판단 기준:**
- 선행 그래프에 **사이클이 없으면** → 충돌 직렬 가능 ✅
- 선행 그래프에 **사이클이 있으면** → 충돌 직렬 불가능 ❌

---

## 트랜잭션 장애와 스케줄 복구

### 스케줄의 복구 가능성 분류

```
Recoverable Schedule (가장 넓음, 모두 포함)
    ↑ 그 안에
Cascadeless Schedule
    ↑ 그 안에
Strict Schedule
    ↑ 그 안에
Serial Schedule (가장 좁음, 가장 엄격)
```

### Recoverable Schedule (복구 가능 스케줄)

Tj가 Ti가 쓴 데이터를 읽은 경우, **Ti의 commit이 Tj의 commit보다 먼저** 실행되어야 한다.

- Ti가 실패하면 Ti에 의존하는 Tj도 롤백해야 하는데, Tj가 먼저 commit되면 복구 불가능
- **Nonrecoverable Schedule**: Tj가 Ti보다 먼저 commit → 복구 불가능

> 복구 가능한 스케줄이 되려면, T7이 T6보다 먼저 commit하면 안 된다 (T7은 T6에 의존)

### Cascadeless Schedule (연쇄 없는 스케줄)

Tj가 Ti가 쓴 데이터를 읽으려면, **Ti의 commit이 Tj의 read보다 먼저** 실행되어야 한다.

- Uncommitted 상태의 데이터를 읽는 것(**Dirty Read**)을 방지
- commit되지 않은 데이터를 읽으면, 해당 트랜잭션 실패 시 연쇄 롤백 발생

```
Ti       Tj
read(X)
write(X)
commit
         read(X)   ← Ti commit 후에 읽어야 Cascadeless
```

### Cascading Rollback (연쇄 롤백)

단일 트랜잭션의 실패가 연쇄적인 롤백을 유발하는 현상.

예시:
- T8 실패 → T8 롤백
- T9가 T8에 의존 → T9 롤백
- T10이 T9에 의존 → T10 롤백

> 연쇄 롤백은 상당한 양의 작업을 취소하므로 바람직하지 않다. Cascadeless Schedule로 방지 가능.

### Strict Schedule (엄격한 스케줄)

트랜잭션이 쓴 데이터는 해당 트랜잭션이 **commit 또는 abort되기 전까지** 다른 트랜잭션이 read/write할 수 없는 스케줄.

- Uncommitted write 이후의 read와 write를 모두 금지
- Strict Schedule은 Serial Schedule의 성질을 보장

```
Ti        Tj
read(X)
read(Y)
write(Y)            ← T1이 write Y → T1 commit 전까지 T2 접근 불가
write(X)
commit
          commit
```

---

## 정리 — 스케줄 비교

| 스케줄 종류 | 조건 | 특징 |
|-------------|------|------|
| **Serial** | 트랜잭션이 순차 실행 | 항상 일관성 보장, 성능 낮음 |
| **Serializable** | 직렬 스케줄과 동등 | 동시 실행 + 일관성 보장 |
| **Recoverable** | Ti commit 후 Tj commit | 복구 가능 |
| **Cascadeless** | Ti commit 후 Tj read | Dirty Read 방지, 연쇄 롤백 방지 |
| **Strict** | Ti commit/abort 후 read/write | 복구 가장 용이 |
