# 동시성 제어 (Concurrency Control)

---

## 동시성 이상현상 (Concurrency Anomalies)

### Dirty Write

**커밋되지 않은(Uncommitted) 상태의 데이터를 다른 트랜잭션이 덮어쓰는(Overwrite) 현상**

```
T1                      T2
read(X=100)
X := 200
write(X=200)
                        read(X=200)
                        X := 300
                        write(X=300)   ← Dirty Write
                        commit
rollback                               ← X는 100으로 복원되어야 하지만 300이 남음
```

- Rollback 결과가 잘못됨 (다른 트랜잭션이 쓴 값이 사라질 수 있음)
- Isolation Level에 무관하게 Recoverability와 관련된 문제
- **모든 Isolation Level에서 Dirty Write는 방지된다**

---

### Dirty Read

**커밋되지 않은 상태의 데이터를 다른 트랜잭션이 읽는 현상**

```
T1                      T2
WRITE(X=200)
                        READ(X=200)   ← Dirty Read (아직 commit 안 됨)
                        commit
ROLLBACK               ← X는 100으로 복원되지만 T2는 이미 200을 읽음
```

- 다른 트랜잭션이 아직 커밋하지 않은 값을 읽어 잘못된 결과 발생

---

### Lost Update

**두 트랜잭션이 동시에 같은 데이터를 읽고 수정할 때, 한 트랜잭션의 수정이 다른 트랜잭션에 의해 덮어써지는 현상**

```
T1                      T2
read(A=100)
                        read(A=100)
A := 100 + 20
                        A := 100 + 10
write(A=120)
                        write(A=110)   ← T1의 수정(120)이 T2에 의해 덮어써짐
commit
                        commit
```

- 최종 A의 값: 110 (직렬 실행 시 기대값: 130, 10이 소실됨)
- T1 트랜잭션의 갱신 내용(120)이 T2에 의해 Overwrite됨

---

### Non-Repeatable Read

**같은 트랜잭션 내에서 같은 데이터를 두 번 읽었을 때 값이 달라지는 현상**

```
T1                      T2
read(A=100)
                        write(A=200)
                        commit
read(A=200)            ← 같은 트랜잭션인데 값이 달라짐
commit
```

- 첫 번째 읽기: 100
- 두 번째 읽기: 200
- 값 변경이 반복 읽기에 영향을 줌

---

### Phantom Read

**같은 트랜잭션 내에서 같은 조건으로 두 번 조회했을 때, 두 번째 조회에서 새로운 행이 나타나는 현상**

```
T1                                          T2
SELECT * FROM Accounts WHERE balance >= 150;
→ {B}
                                            INSERT INTO Accounts VALUES ('C', 300);
                                            COMMIT;
SELECT * FROM Accounts WHERE balance >= 150;
→ {B, C}   ← 없던 행이 나타남
```

- 다른 트랜잭션이 데이터를 삽입/삭제하여 조회 결과 집합이 달라지는 문제
- 새로운 데이터가 나타나므로 "유령(Phantom)" 이라고 부름

---

### Read Skew

**여러 데이터를 읽는 동안 다른 트랜잭션이 값을 변경하여, 읽은 값들이 일관성 없는 상태를 나타내는 현상**

```
T1 (읽기)               T2 (이체: A에서 B로 100 이동)
read(A=500)
                        UPDATE A SET balance = A + 100  → A=600
                        UPDATE B SET balance = B - 100  → B=400
                        commit
read(B=400)
```

- T1이 읽은 값: A=500, B=400 → 합계 900 (원래 합계는 1000)
- 일관성 없는 스냅샷을 읽게 되는 문제

---

### Write Skew

**두 트랜잭션이 서로 다른 데이터를 수정하지만, 각자 읽은 값을 기반으로 수정했을 때 전체적으로 무결성 제약이 깨지는 현상**

```
T1 (Alice 당직 해제)    T2 (Bob 당직 해제)
SELECT count(*) FROM doctor WHERE on_call = True;  → 2명
                        SELECT count(*) FROM doctor WHERE on_call = True;  → 2명
UPDATE doctor SET on_call = False WHERE name = 'Alice';
                        UPDATE doctor SET on_call = False WHERE name = 'Bob';
commit;
                        commit;
```

- 규칙: 최소 1명은 당직이어야 함
- 각 트랜잭션은 2명이 있다고 보고 자신의 당직을 해제
- 결과: Alice=false, Bob=false → **당직자 0명** (무결성 위반)
- Serializable 격리 수준이 필요한 대표적 사례

---

## 트랜잭션 격리 수준 (Transaction Isolation Level)

Serializability를 완전히 보장하면 성능이 낮아지므로, 허용 가능한 이상현상 수준에 따라 격리 수준을 선택한다.

| 격리 수준 | Dirty Read | Lost Update | Non-Repeatable Read | Phantom Read | Write Skew |
|-----------|------------|-------------|----------------------|--------------|------------|
| **Read Uncommitted** | 발생 | 발생 | 발생 | 발생 | 발생 |
| **Read Committed** | 방지 | 발생 | 발생 | 발생 | 발생 |
| **Repeatable Read** | 방지 | 방지 | 방지 | 발생 | 발생 |
| **Serializable** | 방지 | 방지 | 방지 | 방지 | 방지 |

> PostgreSQL 기본값: **Read Committed**
> PostgreSQL의 Repeatable Read는 Phantom Read도 방지함

### Read Uncommitted

- 커밋되지 않은 데이터도 읽기 허용
- 가장 낮은 격리 수준
- PostgreSQL에서는 내부적으로 Read Committed와 동일하게 동작

### Read Committed

- 커밋된 데이터만 읽기 허용
- Non-Repeatable Read, Lost Update, Phantom Read 발생 가능
- **PostgreSQL 기본 격리 수준**

### Repeatable Read

- 커밋된 데이터만 읽고, 같은 트랜잭션 내 반복 읽기 결과가 동일함을 보장
- Write Skew 발생 가능 → Serializable 격리 수준 필요
- PostgreSQL에서는 Phantom Read도 방지

```sql
SET SESSION CHARACTERISTICS AS TRANSACTION ISOLATION LEVEL REPEATABLE READ;
SHOW transaction_isolation;
```

### Serializable

- 가장 높은 격리 수준, 직렬 실행과 동일한 결과 보장
- Write Skew 방지
- PostgreSQL은 **SSI(Serializable Snapshot Isolation)** 알고리즘 사용
- 충돌 시 Rollback 발생 가능

```sql
SET SESSION CHARACTERISTICS AS TRANSACTION ISOLATION LEVEL SERIALIZABLE;
SHOW transaction_isolation;
```

---

## 동시성 제어 기법 분류

| 기법 | 종류 | Serializable | Recoverable |
|------|------|-------------|-------------|
| **2PL** | Basic 2PL | ✅ | ❌ |
| | Strict 2PL | ✅ | ✅ |
| | Rigorous 2PL | ✅ | ✅ |
| **MVCC** | Timestamp Ordering | ✅ | - |
| | Snapshot Isolation (SI) | ❌ | - |
| | Serializable SI (SSI) | ✅ | - |
| **OCC** | Validation-Based | ✅ | - |

> **비관적(Pessimistic)**: 충돌이 자주 발생한다고 가정 → 미리 Lock으로 방지 (2PL)
> **낙관적(Optimistic)**: 충돌이 드물다고 가정 → 실행 후 검증 (OCC)

---

## Lock 기반 프로토콜 (Lock-based Protocols)

### Lock의 기본 개념

- 한 트랜잭션이 데이터에 접근하는 동안 다른 트랜잭션이 해당 데이터에 접근하지 못하도록 **상호 배제(Mutual Exclusive)** 보장
- 모든 트랜잭션은 데이터 접근 전 `lock`, 접근 후 `unlock` 수행

| 연산 | 설명 |
|------|------|
| `lock-S(Q)` | 데이터 Q에 **공유 락(Shared Lock)** 요청 — 읽기만 가능 |
| `lock-X(Q)` | 데이터 Q에 **배타 락(Exclusive Lock)** 요청 — 읽기/쓰기 가능 |
| `unlock(Q)` | 데이터 Q의 락 해제 |

### Lock 호환성

| | 공유 락 (S) | 배타 락 (X) |
|---|---|---|
| **공유 락 (S)** | ✅ 호환 | ❌ 비호환 |
| **배타 락 (X)** | ❌ 비호환 | ❌ 비호환 |

- 공유 락끼리는 동시에 보유 가능 (여러 트랜잭션이 동시에 읽기 가능)
- 배타 락은 다른 어떤 락과도 동시에 보유 불가

### 기본 Locking 문제점

- 트랜잭션이 데이터 접근 후 즉시 unlock하면 직렬 가능성이 보장되지 않음
- 너무 일찍 unlock하면 다른 트랜잭션이 불일치 상태의 데이터를 읽을 수 있음

---

## 2PL (Two-Phase Locking Protocol)

기본 Locking의 문제를 해결하기 위한 프로토콜.

### 2PL 규칙

모든 lock/unlock 연산을 두 단계로 나눔:

| 단계 | 설명 |
|------|------|
| **성장 단계 (Growing Phase)** | lock만 획득 가능, unlock 불가 |
| **수축 단계 (Shrinking Phase)** | unlock만 가능, 새로운 lock 획득 불가 |

- **Lock Point**: 마지막 lock을 획득한 시점
- Conflict Serializability 보장

### Basic 2PL의 문제점

- **연쇄 롤백(Cascading Rollback)** 발생 가능
  - T5 실패 → T6, T7도 연쇄적으로 롤백

---

## Strict 2PL Protocol

**배타 락(Exclusive Lock)을 트랜잭션이 커밋할 때까지 유지**

- Uncommitted 상태로 쓴 데이터는 해당 트랜잭션이 커밋하기 전까지 다른 트랜잭션이 읽을 수 없음
- **연쇄 롤백 방지**
- Serializable ✅, Recoverable ✅

```
T1                      T2
lock-S(A)
read(A)
write(A)
lock-X(B)               ← commit 전까지 X락 유지
write(B)
commit
unlock(A)              ← commit 후 unlock
unlock(B)
                        lock-X(A)   ← T1 commit 후에야 접근 가능
                        write(A)
                        commit
                        unlock(A)
```

---

## Rigorous 2PL Protocol

**모든 락(공유 락 포함)을 트랜잭션이 커밋할 때까지 유지**

- Strict 2PL보다 더 엄격
- Serializable ✅, Recoverable ✅

```
T1                      T2
lock-S(A)              ← 성장 단계
read(A)
lock-X(B)
write(A)
commit
unlock(A)              ← 수축 단계 (commit 후)
unlock(B)
                        lock-X(A)   ← T1 commit 후에야 접근 가능
                        write(B)
                        commit
                        unlock(A)
                        unlock(B)
```

---

## Deadlock (교착 상태)

두 트랜잭션이 서로 상대방이 보유한 락을 기다리며 **영원히 진행되지 못하는 상태**

```
T1              T2
lock-X(A)
                lock-X(B)
lock-X(B)  ←  T2가 보유 → 대기
                lock-X(A)  ←  T1이 보유 → 대기
```

- T1은 T2가 B를 해제하길 기다리고, T2는 T1이 A를 해제하길 기다림
- **해결**: 시스템이 두 트랜잭션 중 하나를 강제로 롤백

---

## MVCC (Multiversion Concurrency Control)

### 핵심 개념

- write(Q) 연산마다 Q의 **새로운 버전(version)을 생성**
- 읽기 연산 시 트랜잭션이 시작된 시점의 버전을 선택하여 읽음
- 읽기와 쓰기가 서로를 블로킹하지 않아 **성능 향상**

### 버전 관리

각 버전은 두 가지 메타데이터를 가진다:

| 필드 | 설명 |
|------|------|
| `xmin` | 해당 버전을 생성한 트랜잭션 ID |
| `xmax` | 해당 버전을 삭제/갱신한 트랜잭션 ID (0이면 현재 최신 버전) |

```
xmin  xmax  id  balance
100   200   1   100      ← T100이 생성, T200이 갱신
200   521   1   150      ← T200이 생성, T521이 갱신
521   0     1   200      ← T521이 생성, 현재 최신 버전
1     0     2   500      ← 최초 삽입, 현재 최신 버전
```

- `xmax = 0` 이면 아직 삭제/갱신되지 않은 최신 버전
- 각 트랜잭션은 자신의 시작 시점 기준으로 보여야 할 버전을 선택

```sql
SELECT xmin, xmax, * FROM table;  -- 버전 정보 확인
```

---

## Snapshot Isolation (SI)

### 핵심 아이디어

트랜잭션이 시작할 때 **데이터베이스의 스냅샷**을 부여받고, 동시에 실행 중인 다른 트랜잭션과 완전히 격리된 상태에서 해당 스냅샷을 기반으로 동작한다.

- 읽기 연산 시 동시성 제어 관리자가 Q의 적절한 버전을 선택하여 읽음
- 쓰기는 **Private Space(개인 공간)**에 먼저 수행

### First Committer Wins (Lost Update 방지)

업데이트 트랜잭션의 검증 단계:

1. 트랜잭션은 읽은 값을 Private Space에서 수정
2. Commit 시도 시 해당 데이터의 `xmax` 값 확인
3. `xmax = 0` 이면 (다른 트랜잭션이 먼저 수정하지 않음) → **Commit 허용**
4. `xmax ≠ 0` 이면 (다른 트랜잭션이 이미 수정함) → **Abort**

```
T30                         T31
read(Q)                     read(Q)
write(Q) → Private Space    write(Q) → Private Space
Commit → xmax 확인 (0이면 Commit)
                            Commit → xmax 확인 (≠0이므로 Abort)
```

> **SI의 한계**: Write Skew 방지 불가 → Serializable 격리 수준(SSI) 필요

---

## OCC (Optimistic Concurrency Control)

### 비관적 vs 낙관적 동시성 제어

| | 비관적 (Pessimistic, 2PL) | 낙관적 (Optimistic, OCC) |
|---|---|---|
| **가정** | 충돌이 자주 발생 | 충돌이 드물게 발생 |
| **방식** | 미리 Lock으로 방지 | 실행 후 검증 |
| **Lock** | 있음 | 없음 (성능 유리) |
| **충돌 시** | 대기/블로킹 | 재시도 (Abort) |
| **적합한 경우** | 충돌 빈도 높은 워크로드 | 읽기 위주 워크로드 |

### EOV (Execute → Order → Validate)

OCC의 3단계 처리 흐름:

**1단계 — 실행 (Execute)**
- 트랜잭션을 실행하되, 실제 DB에 반영하지 않음
- 읽은 데이터의 Key → **Read Set** 기록
- 쓴 데이터의 Key → **Write Set** 기록

```
T20
Read Set:  id:1001 v0, id:2010 v0
Write Set: id:2010 value:5900
```

**2단계 — 순서 결정 (Order)**
- 커밋 순서 결정
- 예: T20 → T31 → T40

**3단계 — 검증 및 커밋 (Validate)**
- 각 트랜잭션의 Read Set에 있는 데이터 버전이 아직 유효한지 검증
- 유효하면 **Commit**, 다른 트랜잭션이 먼저 수정했으면 **Abort**

```
T20: Read Set 버전 유효 → Commit → id:2010 = 5900 (v1)
T31: Read Set에 id:2010 v0 → 이미 v1으로 변경됨 → Abort
T40: Read Set에 id:2010 v0 → 이미 v1으로 변경됨 → Abort
```

---

## 성능 측정 — pgbench

```bash
pgbench -h localhost -p 5432 -U dweb -d benchmark -f order_workload.sql -n -c 10 -j 4 -T 60
```

| 옵션 | 설명 |
|------|------|
| `-h localhost` | 접속할 DB 호스트 |
| `-p 5432` | PostgreSQL 포트 |
| `-U dweb` | 접속 사용자명 |
| `-d benchmark` | 대상 데이터베이스 이름 |
| `-f insert_data.sql` | 실행할 SQL 파일 |
| `-c 10` | 동시 클라이언트 수 |
| `-j 4` | 워커 스레드 수 (-c 10 -j 4: 10개 클라이언트를 4개 스레드로 처리) |
| `-T 60` | 실행 시간(초) (-T 300: 5분 동안 실행) |

**측정 지표:**
- **TPS (Transactions Per Second)**: 초당 처리한 트랜잭션 수
- **Latency**: 단일 트랜잭션 완료까지 걸린 시간

```bash
PGPASSWORD=1234 psql -h localhost -p 5432 -U dweb -d benchmark -f insert_data.sql
PGPASSWORD=1234 bash run_benchmark.sh
```
