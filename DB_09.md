# 정규화 (Normalization)

## 정규화란?

**함수 종속성**을 이용해 릴레이션을 연관성이 있는 속성들로만 구성되도록 분해하여, 이상현상(Anomaly)이 발생하지 않는 올바른 릴레이션으로 만들어 가는 과정이다.

- 관련이 없는 함수 종속성은 별개의 릴레이션에 표현해야 한다.

---

## 주의사항 — 무손실 분해 (Lossless Decomposition)

정규화를 통해 릴레이션을 분해할 때는 반드시 **무손실 분해**가 되어야 한다.

> **무손실 분해**: 분해된 릴레이션들을 다시 자연 조인(Natural Join)했을 때, 원래의 릴레이션과 **동일한 결과**가 나와야 한다.
> - 튜플이 사라지거나 새로운 튜플이 생겨서는 안 된다.
> - 복원 가능한 동등한 릴레이션으로 분해되어야 한다.

---

## 정규형 (Normal Form, NF)

- 릴레이션이 정규화된 **정도**를 나타낸다.
- 각 정규형마다 만족해야 하는 **제약조건**이 존재한다.
- 정규형의 차수가 높아질수록 요구되는 제약조건이 많아지고 더 **엄격**해진다.
- 릴레이션의 특성을 고려하여 **적합한 정규형을 선택**해야 한다.
- 실무에서는 보통 **제3정규형(3NF)** 또는 **BCNF**에서 마무리한다.

### 정규형의 분류

| 분류 | 정규형 |
|------|--------|
| 기존 정규형 | 1NF, 2NF, 3NF, BCNF |
| 고급 정규형 | 4NF, 5NF |

> **주의**: 높은 정규형을 만족하더라도 설계에 따라 이상현상이 발생할 수 있다.

---

## 제1정규형 (1NF, First Normal Form)

### 정의

릴레이션의 **모든 속성 값이 원자값(Atomic Value)** 만 가져야 한다.
즉, 더 이상 분해될 수 없는 단일 값이어야 한다.

- 관계형 DB에서 릴레이션이 갖춰야 할 **최소 조건**이다.
- 1NF만으로는 함수 종속성이 유일하게 적용된다는 보장이 없다.

### 1NF 위반 예시

아래 릴레이션은 `수강과목` 속성에 **다중값(Multi-valued)**이 존재하므로 1NF를 위반한다.

| 학번 | 학생명 | 수강과목 |
|------|--------|----------|
| 2021001 | 김철수 | 데이터베이스, 운영체제 |
| 2021002 | 이영희 | 자료구조 |
| 2021003 | 박민준 | 알고리즘, 네트워크, 컴파일러 |

> `수강과목` 컬럼에 여러 값이 들어가 있어 원자값 조건을 위반한다.

### 1NF 변환 후

| 학번 | 학생명 | 수강과목 |
|------|--------|----------|
| 2021001 | 김철수 | 데이터베이스 |
| 2021001 | 김철수 | 운영체제 |
| 2021002 | 이영희 | 자료구조 |
| 2021003 | 박민준 | 알고리즘 |
| 2021003 | 박민준 | 네트워크 |
| 2021003 | 박민준 | 컴파일러 |

> 모든 속성이 원자값을 가지므로 1NF를 만족한다.
> 단, 이 상태에서 **부분 함수 종속**이 존재할 수 있어 2NF 위반 가능성이 생긴다.

### 1NF 이상현상 예시

기본키가 `(학번, 수강과목)` 인 상태에서 `학생명`은 `학번`만으로 결정된다 → 부분 함수 종속 존재.

| 학번 (PK) | 학생명 | 수강과목 (PK) |
|-----------|--------|--------------|
| 2021001 | 김철수 | 데이터베이스 |
| 2021001 | 김철수 | 운영체제 |
| 2021003 | 박민준 | 알고리즘 |

- **삽입 이상**: 수강과목이 없는 신입생은 기본키 완성이 안 되어 삽입 불가
- **삭제 이상**: 박민준의 알고리즘 수강을 취소하면 박민준의 학생 정보 자체가 삭제됨
- **갱신 이상**: 김철수의 이름을 변경하면 해당 학번이 포함된 모든 행을 수정해야 하며, 일부 누락 시 데이터 불일치 발생

---

## 제2정규형 (2NF, Second Normal Form)

### 정의

1NF를 만족하면서, **기본키의 모든 부분 집합에 대한 부분 함수 종속(Partial Functional Dependency)을 제거**한 상태이다.

즉, 모든 비주요 속성(Non-prime Attribute)이 기본키 전체에 **완전 함수 종속**되어야 한다.

> **부분 함수 종속**: 복합 기본키 `(A, B) → C` 에서 `A → C` 또는 `B → C`가 성립하는 경우.

> **완전 함수 종속**: 기본키 전체에 종속되고, 기본키의 일부에는 종속되지 않는 경우.

### 2NF 위반 예시

기본키가 `(학번, 과목코드)` 인 복합키인데, `학생명`은 `학번` 만으로 결정된다 → **부분 함수 종속 발생**

| 학번 (PK) | 과목코드 (PK) | 학생명 | 성적 |
|-----------|-------------|--------|------|
| 2021001 | CS101 | 김철수 | A |
| 2021001 | CS102 | 김철수 | B+ |
| 2021002 | CS101 | 이영희 | A+ |

- `(학번, 과목코드) → 성적` ✅ 완전 함수 종속
- `학번 → 학생명` ⚠️ 부분 함수 종속 → **2NF 위반**

### 2NF 변환 후 — 분해

**학생 릴레이션**

| 학번 (PK) | 학생명 |
|-----------|--------|
| 2021001 | 김철수 |
| 2021002 | 이영희 |

**수강 릴레이션**

| 학번 (PK, FK) | 과목코드 (PK) | 성적 |
|---------------|-------------|------|
| 2021001 | CS101 | A |
| 2021001 | CS102 | B+ |
| 2021002 | CS101 | A+ |

> 부분 함수 종속을 제거하여 각 릴레이션의 비주요 속성이 기본키 전체에 완전 함수 종속된다.

### 2NF 이상현상 예시

2NF를 만족하더라도 **이행 함수 종속**이 남아있으면 이상현상이 발생한다.

| 학번 (PK) | 학과 | 학과장 |
|-----------|------|--------|
| 2021001 | 컴퓨터공학과 | 홍길동 |
| 2021002 | 컴퓨터공학과 | 홍길동 |
| 2021003 | 경영학과 | 김철수 |

- `학번 → 학과` ✅
- `학과 → 학과장` ✅
- `학번 → 학과장` ⚠️ 이행 함수 종속 → **3NF 위반**

- **삽입 이상**: 학생이 없는 신설 학과의 학과장 정보를 삽입할 수 없음
- **삭제 이상**: 경영학과 학생 2021003을 삭제하면 김철수 학과장 정보도 함께 삭제됨
- **갱신 이상**: 컴퓨터공학과 학과장이 바뀌면 해당 학과 학생 수만큼 모든 행을 수정해야 함

---

## 이행 함수 종속 (Transitive Functional Dependency)

### 정의

`A → B`, `B → C` 가 성립할 때, A가 C를 **직접 결정하지 않는데도 간접적으로 결정**되는 것을 말한다.

- B는 기본키가 아닌 **일반 속성**이다.
- 단일 기본키에서도 발생할 수 있다.

> **부분 함수 종속과의 차이**
> - 부분 함수 종속: 복합키의 **일부**로 결정됨 → 2NF로 제거
> - 이행 함수 종속: **일반 속성**을 거쳐 간접 결정됨 → 3NF로 제거

### 예시

| 학번 (PK) | 학과 | 학과장 |
|-----------|------|--------|
| 2021001 | 컴퓨터공학과 | 홍길동 |
| 2021002 | 컴퓨터공학과 | 홍길동 |
| 2021003 | 경영학과 | 김철수 |

- `학번 → 학과` ✅
- `학과 → 학과장` ✅
- `학번 → 학과장` ⚠️ **이행 함수 종속**

`학과장`은 원래 `학과`에 종속되어야 하는데, `학번 → 학과 → 학과장` 경로로 간접 결정되고 있다.

---

## 제3정규형 (3NF, Third Normal Form)

### 정의

2NF를 만족하면서, **이행 함수 종속(Transitive Functional Dependency)을 제거**한 상태이다.

즉, 기본키가 아닌 모든 속성이 기본키에 **직접 종속**되어야 한다.

> **이행 함수 종속**: `A → B`, `B → C` 가 성립할 때 `A → C` 가 간접적으로 성립하는 경우.
> - B가 기본키가 아닌 일반 속성임에도 C를 결정하는 상황이다.

### 3NF 위반 예시

기본키는 `학번`이지만, `학과장`은 `학번`이 아닌 `학과`에 의해 결정된다 → **이행 함수 종속 발생**

| 학번 (PK) | 학과 | 학과장 |
|-----------|------|--------|
| 2021001 | 컴퓨터공학과 | 홍길동 |
| 2021002 | 컴퓨터공학과 | 홍길동 |
| 2021003 | 경영학과 | 김철수 |

- `학번 → 학과` ✅
- `학과 → 학과장` ✅
- `학번 → 학과장` ⚠️ 이행 함수 종속 → **3NF 위반**

### 3NF 변환 후 — 분해

**학생 릴레이션**

| 학번 (PK) | 학과 (FK) |
|-----------|-----------|
| 2021001 | 컴퓨터공학과 |
| 2021002 | 컴퓨터공학과 |
| 2021003 | 경영학과 |

**학과 릴레이션**

| 학과 (PK) | 학과장 |
|-----------|--------|
| 컴퓨터공학과 | 홍길동 |
| 경영학과 | 김철수 |

> 이행 함수 종속을 제거하여 `학과장`이 `학과` 릴레이션에서 직접 관리된다.

### 3NF 이상현상 예시

3NF를 만족하더라도 **결정자가 후보키가 아닌 경우** 이상현상이 발생한다.

한 학생이 여러 과목을 수강할 수 있고, 각 과목은 한 명의 교수만 담당하며, 교수는 하나의 과목만 담당한다고 가정한다.

| 학번 (PK) | 과목 (PK) | 교수 |
|-----------|-----------|------|
| 2021001 | 데이터베이스 | 홍길동 |
| 2021001 | 운영체제 | 김철수 |
| 2021002 | 데이터베이스 | 홍길동 |

- **삽입 이상**: 새 교수가 과목을 배정받았지만 아직 수강 학생이 없으면 삽입 불가 (학번이 없어 기본키 완성 불가)
- **삭제 이상**: 2021001이 운영체제 수강을 취소하면 김철수 교수의 담당 과목 정보도 함께 삭제됨
- **갱신 이상**: 홍길동 교수의 담당 과목이 바뀌면 해당 교수가 포함된 모든 행을 수정해야 하며, 일부 누락 시 데이터 불일치 발생

---

## BCNF (Boyce-Codd Normal Form)

### 정의

3NF를 만족하면서, **모든 결정자(Determinant)가 후보키(Candidate Key)** 여야 한다.

즉, `A → B` 가 성립하는 모든 함수 종속에서 A는 반드시 후보키이어야 한다.

> 3NF보다 엄격한 조건으로, 3NF를 만족하더라도 BCNF를 위반할 수 있다.
> - 3NF는 기본키가 아닌 속성이 결정자가 되는 경우를 허용하지만, BCNF는 허용하지 않는다.

### BCNF 위반 예시

한 학생이 여러 과목을 수강할 수 있고, 각 과목은 한 명의 교수만 담당하며, 교수는 하나의 과목만 담당한다고 가정한다.

기본키: `(학번, 과목)` / 후보키: `(학번, 과목)`, `(학번, 교수)`

| 학번 (PK) | 과목 (PK) | 교수 |
|-----------|-----------|------|
| 2021001 | 데이터베이스 | 홍길동 |
| 2021001 | 운영체제 | 김철수 |
| 2021002 | 데이터베이스 | 홍길동 |

- `(학번, 과목) → 교수` ✅
- `교수 → 과목` ✅ → **교수가 결정자이지만 후보키가 아님** → **BCNF 위반**

> 3NF는 만족하지만 BCNF는 위반하는 상황이다.
> `교수 → 과목` 종속에서 `교수`는 후보키가 아니므로 BCNF 조건 위반이다.

### BCNF 변환 후 — 분해

**수강 릴레이션**

| 학번 (PK) | 교수 (PK, FK) |
|-----------|---------------|
| 2021001 | 홍길동 |
| 2021001 | 김철수 |
| 2021002 | 홍길동 |

**교수 릴레이션**

| 교수 (PK) | 과목 |
|-----------|------|
| 홍길동 | 데이터베이스 |
| 김철수 | 운영체제 |

> 결정자인 `교수`를 후보키로 갖는 별도 릴레이션으로 분리하여 BCNF를 만족한다.

### BCNF 이상현상 예시

BCNF를 만족하더라도 **다치 종속(Multi-valued Dependency)** 이 존재하면 이상현상이 발생한다.

한 교수가 여러 과목을 담당할 수 있고, 한 교수가 여러 책을 사용할 수 있으며, 과목과 책은 독립적이라고 가정한다.

| 교수 (PK) | 과목 (PK) | 교재 (PK) |
|-----------|-----------|-----------|
| 홍길동 | 데이터베이스 | DB개론 |
| 홍길동 | 데이터베이스 | SQL완전정복 |
| 홍길동 | 운영체제 | DB개론 |
| 홍길동 | 운영체제 | SQL완전정복 |

- **삽입 이상**: 홍길동 교수가 새 교재를 추가하면 담당하는 모든 과목 조합으로 행을 삽입해야 함
- **삭제 이상**: 특정 과목-교재 조합을 삭제하면 다른 조합도 일관성 유지를 위해 함께 삭제해야 함
- **갱신 이상**: 교재명이 바뀌면 해당 교재가 포함된 모든 행을 수정해야 함

> BCNF 위반이 아니라 **4NF 위반**에 해당하며, 다치 종속을 제거해야 한다.

### 3NF vs BCNF 비교

| 구분 | 3NF | BCNF |
|------|-----|------|
| 이행 함수 종속 | 제거 | 제거 |
| 결정자 조건 | 완화 (일부 허용) | 엄격 (모두 후보키여야 함) |
| 무손실 분해 보장 | ✅ | ✅ |
| 종속성 보존 보장 | ✅ | ❌ (항상 보장되지 않음) |
| 실무 적용 | 일반적 | 종속성 손실 가능성 검토 후 적용 |

---

## 제4정규형 (4NF, Fourth Normal Form)

### 정의

BCNF를 만족하면서, **다치 종속(Multi-valued Dependency, MVD)을 제거**한 상태이다.

> **다치 종속**: 속성 A가 속성 B의 값 집합을 독립적으로 결정하고, 동시에 속성 C의 값 집합도 독립적으로 결정할 때 `A →→ B` 로 표기한다.
> - B와 C가 서로 독립적임에도 하나의 테이블에 함께 존재하는 경우이다.

### 4NF 위반 예시

한 교수가 여러 과목을 담당하고, 여러 교재를 사용하며, 과목과 교재는 서로 독립적이라고 가정한다.

| 교수 | 과목 | 교재 |
|------|------|------|
| 홍길동 | 데이터베이스 | DB개론 |
| 홍길동 | 데이터베이스 | SQL완전정복 |
| 홍길동 | 운영체제 | DB개론 |
| 홍길동 | 운영체제 | SQL완전정복 |

- `교수 →→ 과목` (교수가 과목 집합을 독립적으로 결정)
- `교수 →→ 교재` (교수가 교재 집합을 독립적으로 결정)
- 과목과 교재는 무관하지만 하나의 테이블에 묶여 모든 조합의 행이 생성됨 → **4NF 위반**

### 4NF 변환 후 — 분해

**교수-과목 릴레이션**

| 교수 | 과목 |
|------|------|
| 홍길동 | 데이터베이스 |
| 홍길동 | 운영체제 |

**교수-교재 릴레이션**

| 교수 | 교재 |
|------|------|
| 홍길동 | DB개론 |
| 홍길동 | SQL완전정복 |

> 다치 종속을 제거하여 독립적인 관계를 별도 릴레이션으로 분리한다.

---

## 제5정규형 (5NF, Fifth Normal Form)

### 정의

4NF를 만족하면서, **조인 종속(Join Dependency, JD)을 제거**한 상태이다.

> **조인 종속**: 릴레이션 R이 자신의 프로젝션들을 자연 조인한 결과와 동일할 때, 조인 종속이 존재한다고 한다.
> - 즉, 세 개 이상의 릴레이션으로 분해했다가 다시 조인했을 때 원래 릴레이션이 완벽히 복원되는 경우이다.

### 5NF 위반 예시

공급자, 부품, 프로젝트의 삼자 관계에서 다음 업무 규칙이 모두 성립한다고 가정한다.

- 공급자 S가 부품 P를 공급할 수 있다
- 프로젝트 J가 부품 P를 사용한다
- 공급자 S가 프로젝트 J에 참여한다

| 공급자 | 부품 | 프로젝트 |
|--------|------|----------|
| S1 | P1 | J1 |
| S1 | P1 | J2 |
| S1 | P2 | J1 |
| S2 | P1 | J1 |

- 세 속성의 조합이 아닌 두 속성씩의 조합으로도 정보가 충분히 표현 가능 → **조인 종속 존재 → 5NF 위반**

### 5NF 변환 후 — 분해

**공급자-부품 릴레이션**

| 공급자 | 부품 |
|--------|------|
| S1 | P1 |
| S1 | P2 |
| S2 | P1 |

**부품-프로젝트 릴레이션**

| 부품 | 프로젝트 |
|------|----------|
| P1 | J1 |
| P1 | J2 |
| P2 | J1 |

**공급자-프로젝트 릴레이션**

| 공급자 | 프로젝트 |
|--------|----------|
| S1 | J1 |
| S1 | J2 |
| S2 | J1 |

> 세 릴레이션을 자연 조인하면 원래 릴레이션이 복원된다 (무손실 분해).

---

## 정규형 전체 요약

| 정규형 | 제거 대상 | 조건 |
|--------|-----------|------|
| **1NF** | 다중값 속성 | 모든 속성이 원자값 |
| **2NF** | 부분 함수 종속 | 비주요 속성이 기본키 전체에 완전 함수 종속 |
| **3NF** | 이행 함수 종속 | 비주요 속성이 기본키에 직접 종속 |
| **BCNF** | 후보키가 아닌 결정자 | 모든 결정자가 후보키 |
| **4NF** | 다치 종속 | 후보키에 의한 다치 종속만 허용 |
| **5NF** | 조인 종속 | 후보키에 의한 조인 종속만 허용 |
